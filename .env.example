# Frontend -> backend wiring (dev)
#
# In `pnpm dev`, Vite will proxy these to avoid CORS:
# - /__auth      -> VITE_AUTH_SERVICE_BASE_URL
# - /__keycloak  -> VITE_KEYCLOAK_BASE_URL

VITE_AUTH_SERVICE_BASE_URL=http://localhost:8082
VITE_KEYCLOAK_BASE_URL=http://localhost:8081
VITE_KEYCLOAK_REALM=creatorOs
VITE_KEYCLOAK_CLIENT_ID=auth-service
# Only set if your Keycloak client is confidential (not recommended for SPA)
# VITE_KEYCLOAK_CLIENT_SECRET=